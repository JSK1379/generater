# Repository workspace

The consolidated Near Ride project lives in:

```text
near-ride/
```

All monorepo refactor work remains on `refactor/near-ride-monorepo` until explicitly approved for merge into `main`.
